services:
  api:
    build:
      context: ./apps/api
    ports:
      - "${API_PORT:-8000}:8000"
    env_file:
      - .env

  web:
    build:
      context: ./apps/web
    ports:
      - "3000:3000"
    env_file:
      - .env
    depends_on:
      - api
